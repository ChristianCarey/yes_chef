<h4><%= link_to "Add a menu item", new_menu_item_path %></h4>
<hr>
<% if @menu_items.size > 0 %>
  <%= render partial: 'menu_item', collection: @menu_items, as: :menu_item %>
<% else %>
  <h3>No Menu Items</h3>
<% end %>
