
<%= content_for :sidenav, true %>
<% if @items.size > 0 %>
  <%= render partial: 'menu_item', collection: @items %>
  <%=link_to "Create Menu Item", new_menu_item_path %>
<% else %>
  <h3>No Menu Items</h3>
<% end %>

